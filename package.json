{
  "name": "chicken-banana",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "server": "npm run dev --prefix server",
    "dev:all": "npx -y concurrently \"npm run dev\" \"npm run server\"",
    "build": "vite build",
    "start": "NODE_ENV=production node server/index.js",
    "preview": "vite preview",
    "postinstall": "cd server && npm install"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {}
}