// Supabase-based admin creation removed.
// Use the included local scripts to create an admin account for development.

console.log('This script no longer uses Supabase. To create a local admin:')
console.log('- Run: node scripts/create-local-admin.js <email>')
console.log('- Or run the SQL in scripts/grant-admin.sql against your local DB')
process.exit(0)
